<UserControl x:Class="ProcesadoSummary.Views.GenerarTDView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:ProcesadoSummary.ViewModel"
             xmlns:local="clr-namespace:ProcesadoSummary.Views"
             mc:Ignorable="d"
             Background="White"
             d:DesignHeight="600" d:DesignWidth="700">

    <UserControl.DataContext>
        <vm:GenerarTDViewModel/>
    </UserControl.DataContext>

    <Grid>
        <TextBlock Text="Generar tablas cada 10m" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Top" Cursor="" />

    </Grid>
</UserControl>
